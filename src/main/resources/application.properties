spring.application.name=backend

# =====================================================
# MSSQL 資料庫連線設定
# =====================================================
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=EcommerceDB;encrypt=false
spring.datasource.username=sa
spring.datasource.password=1qaz@WSX
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# =====================================================
# JPA / Hibernate 設定
# =====================================================
# 開發階段自動建立 / 更新資料表
spring.jpa.hibernate.ddl-auto=create
# spring.jpa.hibernate.ddl-auto=update

# 顯示 SQL 語法，方便 Debug
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 指定 SQL Dialect，使用 SQL Server
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# =====================================================
# 綠界測試帳號
# =====================================================

ecpay.merchantId=2000132
ecpay.hashKey=5294y06JbISpM5x9
ecpay.hashIV=v77hoKGq4kWxNNIS
ecpay.testUrl=https://payment-stage.ecpay.com.tw/Cashier/AioCheckOut/V5

# =====================================================
# Server 監聽埠號（可選，預設 8080）
# =====================================================
server.port=8080
